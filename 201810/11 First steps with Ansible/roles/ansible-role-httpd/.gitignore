# .gitignore

# Hidden Vagrant-directory
.vagrant

# Backup files (e.g. Vim, Gedit, etc.)
*~

# Vagrant base boxes (you never know when someone puts one in the repository)
*.box

*-tests

# ignore windows notepad++ backup files. When file modified creates dir nppBackup with backuped changed files
nppBackup/